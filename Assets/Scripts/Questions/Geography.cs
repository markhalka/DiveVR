using UnityEngine;

public class Geography : MonoBehaviour
{
    //ok, so just add this real quick because why not 
    //what you need to do, is check when the user clicks on a sprite, if yes add an animtion and show the lable 
    //then you can create a seperate quiz thing, just like before, and you are done 
    //actually, just literally put this in science modles 

    //create a prefab with the sprites? or recreatet it 

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
